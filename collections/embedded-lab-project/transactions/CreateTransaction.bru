meta {
  name: CreateTransaction
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}:{{PORT}}/transactions
  body: json
  auth: inherit
}

body:json {
  {
    "rfid": "new-rfid",
    "type": "C"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
